import React from 'react';
import { SearchButton } from './index';

export default {
  title: 'app/tamaguiComponents/SearchButton',
  component: SearchButton,
  parameters: { status: { type: 'beta' } },
};

export const main = () => <SearchButton />;
