import React from 'react';
import { IconStack } from './index';

export default {
  title: 'ui/tamagui/IconStack',
  component: IconStack,
  parameters: { status: { type: 'beta' } },
};

export const main = () => <IconStack>hello</IconStack>;
