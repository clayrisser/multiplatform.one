import React from 'react';
import { SponsorButton } from './index';

export default {
  title: 'ui/tamagui/SponsorButton',
  components: SponsorButton,
  parameters: { status: { type: 'beta' } },
};
export const main = () => <SponsorButton />;
