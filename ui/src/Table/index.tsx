import { Table as RNTable, TableProps } from 'react-native-table-component';
import { styled, YStack, Text } from 'tamagui';

// export const Table = styled(RNTable, { ...TableProps });

export const App = () => {
  return (
    <YStack>
      <Text>Hello</Text>
    </YStack>
  );
};
