import React from 'react';
import { CheckCircle } from './index';

export default {
  title: 'ui/tamagui/CheckCircle',
  component: CheckCircle,
  parameters: { status: { type: 'beta' } },
};

export const main = () => <CheckCircle />;
